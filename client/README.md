# Cliente PySide6 (PedePao)

- Bloqueia pedidos após o cutoff (13:00) no fuso `America/Sao_Paulo`.
- Lista produtos ativos da API (`/api/products`).

## Rodando
```bash
pip install -r requirements.txt
python app.py
```
